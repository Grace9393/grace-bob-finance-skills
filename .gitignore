# Exclude local-only dev scripts and package requirements
# Process Studio static scanner flags any .py file containing package name strings
# (playwright, pypdf, PIL etc.) regardless of try/except or importlib usage
skills/*/scripts/
skills/*/requirements.txt

# Windows metadata
desktop.ini
